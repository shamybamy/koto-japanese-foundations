version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 24.19.0
        - nvm use 24.19.0
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
